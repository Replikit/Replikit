namespace Replikit.Core.Controllers;

public static class ReplikitControllerProperties
{
    private const string Prefix = nameof(ReplikitControllerProperties);

    public const string CommandGroupName = Prefix + nameof(CommandGroupName);
}
