namespace Replikit.Core.Controllers;

public static class ReplikitEndpointProperties
{
    private const string Prefix = "Replikit:";

    public const string CommandName = Prefix + nameof(CommandName);
}
