namespace Replikit.Extensions.Views;

public static class ViewEndpointProperties
{
    private const string Prefix = nameof(ViewEndpointProperties) + ":";

    public const string IsAction = Prefix + nameof(IsAction);
    public const string AllowExternalActivation = Prefix + nameof(AllowExternalActivation);
}
